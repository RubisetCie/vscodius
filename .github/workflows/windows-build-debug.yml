name: Windows Build Debug
on:
  workflow_dispatch:
    inputs:
      pkg_arch:
        description: 'Target package architecture (x64 or ia32)'
        required: true
        default: 'x64'
      vs_arch:
        description: 'Target MSVC architecture (x64, x86, etc)'
        required: true
        default: 'x64'
      node_version:
        description: 'Node.js version'
        required: true
        default: '16.x'
      patch_resources:
        description: 'Patch Win32 resources after build'
        type: boolean
        required: true
        default: false
jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Set up Yarn
        uses: mskelton/setup-yarn@v1
        with:
          node-version: '${{github.event.inputs.node_version}}'
      - name: Set up MSVC
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: '${{github.event.inputs.vs_arch}}'
          spectre: false
          uwp: false
      - name: Compilation
        run: yarn compile
      - name: Packaging
        run: yarn gulp vscode-win32-${{github.event.inputs.pkg_arch}}
      - name: Patch resources
        if: ${{github.event.inputs.patch_resources == 'true'}}
        run: node build\win32-resources-patch ..\VSCode-win32-${{github.event.inputs.pkg_arch}}/VSCodius.exe
      - name: Move artifact
        run: move /Y ..\VSCode-win32-${{github.event.inputs.pkg_arch}} .\artifact
      - name: Upload artifact
        uses: actions/upload-artifact@v3
        with:
          name: VSCodius-Win32-${{github.event.inputs.pkg_arch}}-Debug.zip
          path: 'artifact'
          if-no-files-found: error